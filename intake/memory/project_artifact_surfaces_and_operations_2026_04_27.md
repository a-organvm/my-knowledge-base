---
name: Surfaces principle + Canonical operations registry
description: 2026-04-27 — every artifact has 5 surfaces (internal/client/public/visual/theoretical); recurring prompts cleaned into ordered phase pipeline + variant axes + pillar registry + 8 persona lenses
type: project
originSessionId: 81498485-45e0-487a-8140-0c4e281fd377
---
**What:** Captures two reframes that arrived after the meta-processor spec —
1. **Surfaces principle**: every artifact exists in 5 simultaneous faces (S1 internal/operator, S2 client, S3 public/audience, S4 visual/spatial, S5 theoretical container). Producing only S1 and treating it as "the work" is the trap. The Personalized Client Storefront Substrate v1 was the seed S2 renderer; needs analogues for S3-S5.
2. **Canonical operations registry**: the user's recurring prompts (`$NEXT`, `$MODE`, `$DEPTH`, `$PILLAR_*`, `$REPO_ARCHITECTURE_*`) cleaned into a 7-phase pipeline (research positioning → atlas → spec → positioning → implementation → validation → scale) with 5 variant axes (depth, mode, surface, audience, rigor), a 5-pillar rigor registry (causal inference, unit economics, algorithmic interface, constraint-domain, methodological rigor), and an 8-lens persona registry (pathologist, ethicist, domain expert, strategist, engineer, historian, anthropologist, critic).

**Where:** `~/.claude/plans/2026-04-27-surfaces-and-canonical-operations-registry.md`

**Project:** ORGANVM meta-substrate (extends the meta-processor spec by adding surfaces axis + operations axis + persona-lens axis)

**For whom:** Anthony — addresses two specific signals: "every surface requires a public face and therefore requires other faces as well" and "we need cleaning and making logical [the recurring prompts] ... we want to be experts in the field. We want to be pathologists, ethicists, and so forth."

**State:** Spec captured; none of the surface renderers (S3/S4/S5), operation invocation harness, persona-lens prompt templates, or format-as-container scaffold library are built yet.

**Critical insight (format-as-container):** A "10-12 slide pitch deck" is not just an output, it's a *theoretical commitment with structural work* — the shape forces problem→opportunity→solution→traction→ask compression before content fills it. Same for one-pager (radical compression to one glance), repo architecture (separation-of-concerns + composability), research atlas (pillars × sources × milestones × outputs). §5 of the artifact catalogs 15 such containers.

**Three highest-leverage runs identified (§4.2):**
1. `run(positioning-pitch-deck, depth=operator, surface=S2+S3+S4, lens=strategist, entity=Rob)` — Rob has dense substrate, weak pitch artifact; one deck satisfies 3 surfaces
2. `run(specification-repo-architecture, depth=dissertation, surface=S5-theoretical, lens=engineer+historian, entity=meta-processor-itself)` — formalize the meta-processor as portable container so Jessica/Micah/Pace plug in cheaply
3. `run(validation-experiment-design, depth=lean, surface=S1-internal, lens=pathologist, entity=JCPenney-negative-space)` — the JCPenney intake test from meta-processor §7.6

**Composition rule:** every operation invocation = `run(op, depth, mode, surface, audience, rigor)` against `(entity, substrate-row, cascade-layer)` with persona-lens overlay. Total dimensionality ~3.4M cells per entity; the §4.1 collapse rule reduces to 9×7×5×8 = 2,520 invocations as a tractable menu.

**Surface coverage scored per current entity (§2.1):** Rob+Maddie strong on S1, partial S2/S4; Jeff Graves partial S1; Jessica/Micah/Pace/Family-Industries absent everywhere; JCPenney absent (but n/a for S2 since negative-space doesn't deliver to clients).

**Pending feedback:** None. Spec captured; user-directed next moves.

**Sister artifacts (this session, in order):**
1. `rob-gemini-warm-clock.md` — Rob lane operational substrate
2. `2026-04-27-rob-maddie-trailing-artifact-catalog.md` — cross-session inventory
3. `2026-04-27-keyword-extraction-and-drive-content-map.md` — function vs domain language; 9 Drive lanes
4. `2026-04-27-meta-processor-spec-instances-to-universal-substrate.md` — Maddie/Rob as instances; 13×7 cell matrix
5. **this** — surfaces × operations × lenses (full action space)

Together: a five-artifact composable substrate that names every axis of the same processor.
